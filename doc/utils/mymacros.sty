%!TEX encoding = UTF-8 Unicode
%!TeX spellcheck = it_IT

\ProvidesPackage{mymacros}[08/9/2017 v1.0 My own macros]

%----------------------------------------------------------------------------------------
%	TEXT AND WORDS
%----------------------------------------------------------------------------------------
\newcommand{\Cpp}{C\texttt{++} }

%----------------------------------------------------------------------------------------
%	MATH ENVIROMENT
%----------------------------------------------------------------------------------------
% Asymptotic notation
\newcommand{\BigO}[1]{$\mathcal{O}(#1)$}
\newcommand{\BigOmega}[1]{$\Omega(#1)$}
\newcommand{\BigTheta}[1]{$\Theta(#1)$}

%----------------------------------------------------------------------------------------
%	ALGORITHMS AND PSEUDOCODED
%----------------------------------------------------------------------------------------
% BState
\makeatletter
\def\BState{\State\hskip-\ALG@thistlm}
\makeatother

% ForEach
\algnewcommand\algorithmicforeach{\textbf{for each}}
\algdef{S}[FOR]{ForEach}[1]{\algorithmicforeach\ #1\ \algorithmicdo}

% Italian language
\newenvironment{italianalgorithm}[1][]
  {\begin{algorithm}[#1]
     \selectlanguage{italian}%
     \floatname{algorithm}{Algoritmo}%
     \renewcommand{\algorithmicif}{\textbf{se}}%
     \renewcommand{\algorithmicthen}{\textbf{allora}}%
		 \renewcommand{\algorithmicelse}{\textbf{altrimenti}}%
     \renewcommand{\algorithmicend}{\textbf{fine}}%
		 \renewcommand{\algorithmicforeach}{\textbf{per ogni}}%
		 \renewcommand{\algorithmicdo}{\textbf{fai}}%
		 \renewcommand{\algorithmicprocedure}{\textbf{procedura}}%
     % Set other language requirements
  }
  {\end{algorithm}}

% Text paragraph
\makeatletter
\newcommand{\algmargin}{\the\ALG@thistlm}
\makeatother
\algnewcommand{\TextState}[1]{\State%
  \parbox[t]{\dimexpr\linewidth-\algmargin}{\strut #1\strut}}
